/*import { Route, Navigate } from "react-router-dom";
import PublicRoute from "./routes";



export const PrivateRoute = ({user, Element, ...rest}) => {
  return (
    <Route {...rest}
    element = {(props) =>
    user ? <Element {...props} /> : <Navigate to= "/" />
    }
    />

  );
}
*/